[project]
name = "financial-leak-env"
version = "0.1.0"
description = "OpenEnv Financial Leak Detection Environment"
authors = [
    { name="Kena Parmar" }
]
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "python-dotenv",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "api.app:app"